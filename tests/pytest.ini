[pytest]
addopts =
    -n auto
    --strict-markers
    --strict-config
    --dist=loadgroup
    -ra
    -v
xfail_strict = true
testpaths = tests
pythonpath = ..
markers =
    smoke: mark a test as a smoke test.